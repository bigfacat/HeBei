var apiUrl = "/ucenter/api";
var urlContext = "/ucenter";

//mini框架弹窗位置控制
var showWindowX = "center"; //显示编辑框X位置
var showWindowY = "top"; //显示编辑框Y位置

/*$(document).ajaxComplete(function(evt, request, settings) {
	var result = mini.decode(request.responseText);
	if (result.message == "ajaxSessionTimeOut") {
		mini.alert("登录超时，请重新登录。", "提示信息", function() {
			window.parent.location.href = urlContext + "/YhglPlat/index.aspx"; 
		});
	}
});*/
